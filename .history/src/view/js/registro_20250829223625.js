document.addEventListener("DOMContentLoaded", () => {

    const formulario = document.getElementById("formulario_registro");

    formulario.addEventListener("submit", async(e) => {
        e.preventDefault();

        const nombre = document.getElementById("inicio_nombre");
        const apellido = document.getElementById("inicio_apellido");
        const correo = document.getElementById("inicio_correo");
        const contraseña = document.getElementById("inicio_");

    });

});