document.addEventListener("DOMContentLoaded", () => {

    const formulario = document.getElementById("formulario_registro");

    formulario.addEventListener("submit", async(e) => {
        e.preventDefault();

        const nombre = document.getElementById("inicio");
        const apellido = document.getElementById("inicio");
        const correo = document.getElementById("inicio");
        const contraseña = document.getElementById("inicio");

    });

});