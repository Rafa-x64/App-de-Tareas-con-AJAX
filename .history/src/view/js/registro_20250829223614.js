document.addEventListener("DOMContentLoaded", () => {

    const formulario = document.getElementById("formulario_registro");

    formulario.addEventListener("submit", async(e) => {
        e.preventDefault();

        const nombre = document.getElementById("inicio_");
        const apellido = document.getElementById("inicio_");
        const correo = document.getElementById("inicio_");
        const contraseña = document.getElementById("inicio_");

    });

});